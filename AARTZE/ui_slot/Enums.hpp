#pragma once

enum class UiVisibility { Visible, HitTestInvisible, SelfHitTestInvisible, Collapsed };

enum class UiHAlign { Fill, Left, Center, Right };
enum class UiVAlign { Fill, Top, Center, Bottom };

enum class UiOrientation { Horizontal, Vertical };
enum class UiSizeRule { Auto, Fill };

